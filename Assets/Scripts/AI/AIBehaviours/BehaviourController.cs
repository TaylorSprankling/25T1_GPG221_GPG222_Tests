using UnityEngine;

public class BehaviourController : MonoBehaviour
{
    [SerializeField] protected MoveForward moveForward;
    [SerializeField] protected Wander wander;
    [SerializeField] protected Avoid avoid;
}
